# 工作記錄系統 v1.2.1 功能規格

## 本版目標

在 v1.2.0 的四日期與 AUTO／MANUAL 保護規則上，新增通用的節點類型與前置任務關聯。

## 節點類型

- `TASK`：有執行期間的工作節點。
- `MILESTONE`：工作日為 0 的里程碑，只代表重要結果或條件已達成。

所有既有節點升級後預設為 `TASK`。

## 前置任務

每個專案節點可以設定：

- 沒有前置任務
- 一個前置任務
- 多個前置任務

前置任務只能選擇同一專案內的其他節點，不可選擇自己。

系統不得依節點名稱或畫面排序自動認定前置關係，也不得寫死任何固定工作流程。

## 關聯方式

v1.2.1 固定使用：

```text
FS：前置任務完成後，本節點才能開始
```

每一筆關聯可設定 `lag_work_days`，預設為 0，且不得小於 0。

## 多個前置任務

當節點有多個前置任務時，必須全部完成後才能開始。

```text
本節點預計開始日
＝ 各前置任務完成日＋各自間隔工作日後
  所得到限制日期中的最晚日期
```

沒有被選為前置任務的平行工作，不得阻擋本節點。

## 循環依賴

禁止：

- A → A
- A → B → A
- A → B → C → A

發現循環時顯示「前置任務形成循環，請重新設定」，並禁止儲存。

## AUTO／MANUAL

- `AUTO`：可由前置任務重新計算日期。
- `MANUAL`：保留使用者輸入或清除的日期。
- MANUAL 日期早於前置任務允許日期時，只顯示衝突警告，不自動覆蓋。

## 重新計算範圍

- 僅本節點
- 本節點及所有相依的後續節點
- 整個專案

「後續節點」依前置任務關聯尋找，不依畫面排序。

## 甘特圖

- TASK 顯示橫向時程條。
- MILESTONE 顯示菱形。
- 顯示前置任務箭頭。
- 平行工作日期可以重疊。
- MANUAL 衝突顯示警告。
- 拖曳後改為 MANUAL。
- 選擇順延後續節點時，只順延相依的後續節點。

## 資料庫

### `project_schedule_nodes`

新增：

```text
node_type: TASK / MILESTONE
```

### `project_schedule_node_dependencies`

欄位包含：

```text
id
project_key
node_id
predecessor_node_id
dependency_type
lag_work_days
created_at
updated_at
created_by
updated_by
deleted_at
```

## 既有資料相容

- 不自動建立前置任務。
- 不自動重新計算既有日期。
- 保留四日期與 AUTO／MANUAL。
- 不建立固定節點、固定前置關係或固定產業流程。
